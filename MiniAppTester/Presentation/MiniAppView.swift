import SwiftUI

struct MiniAppView: View {
    var body: some View {
        VStack {
            Text("MiniApp is running...")
                .font(.title)
                .padding()
        }
    }
}
